---
import { Avatar } from "@lygem/ui"
import Layout from "#layouts/Layout.astro"
import { readConfig } from "#lib/config.js"
const configPath = import.meta.env.LYGEM_CONFIG_PATH
const config = await readConfig(configPath)
---

<Layout title={config.meta.pageTitle}>
	<Avatar src="https://placehold.co/1000" alt="avatar" />
	<p>Under Construction</p>
</Layout>
